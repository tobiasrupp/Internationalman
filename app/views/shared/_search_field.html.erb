<%= form_tag search_path, method: :get, :id => 'search_form' do %>
		<div class="input-append">
    <%= text_field_tag :query, params[:query], :class => "span3", :id => "appendedInputButton" %>
    <%= submit_tag t(:search), name: nil, :class => 'btn btn-mini' %>
		</div>
<% end %>