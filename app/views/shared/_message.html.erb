<% flash.each do |key, msg| %>
	<% if key == :error %>
		<span class="alert alert-error">
	<% else %>
		<span class="alert alert-info">
	<% end %>
  <%= msg %></span>
  <div class="clear"></div>
<% end %>